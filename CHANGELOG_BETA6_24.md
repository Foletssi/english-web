# Eastudy Beta 6.24.0

## 学习目标与自动连播

- 学生首次登录后设置主学习目标、每日学习时间和当前水平。
- 目标档案保存到 Supabase，并按学生账号通过 RLS 隔离；本地缓存只属于当前账号。
- 首页增加“我的主目标”和“开始目标学习”入口。
- 目标队列只使用已发布且具有审核映射的视频；缺少内容的考试路线明确显示“内容建设中”。
- 支持目标队列与固定合集队列、5 秒下一条倒计时、立即播放、取消本次自动播放和整池循环。
- 离开播放器、隐藏页面、退出或进入练习模式时取消倒计时，避免迟到跳转和后台播放。

## 学习证据

- 断点位置与有效观看覆盖分开记录。
- 拖动到结尾只更新播放位置，不再直接标记为完成。
- Supabase `user_progress` 增加观看覆盖区间和覆盖比例字段。

## 数据库

- 新增迁移 `supabase/migrations/20260908_learning_goal_autoplay.sql`。
- 新增目标目录 `learning_targets` 与个人档案 `learner_goal_profiles`。

## 验证

- 新增学习队列与倒计时契约测试。
- 全部认证、内容、云端/R2和静态审计继续通过。
