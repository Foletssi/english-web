# Eastudy 学员登录与注册设计方案

日期：2026-09-07  
适用版本：本地 Beta6.20.5；验证码前端与客户端逻辑已实施，真实短信仍需配置供应商。  
调研依据：Supabase、Firebase 官方文档。文末附来源；以下界面安排和参数是针对 Eastudy 的建议，并非对全行业使用比例的统计。

## 1. 建议采用的方式

采用“验证码登录与注册合并，密码登录可选”。这是成熟认证平台支持的常见方式，适合首次使用门槛较低的学习网站。

学员输入手机号、收到短信、填写正确验证码后：已有账号直接登录；新手机号完成首次注册并登录。以后可以继续使用“手机号＋本次收到的验证码”，不强制设置密码。设置密码后，再增加“手机号＋密码”这一种登录方式，两种方式共用同一个账号和学习数据。

只有输入手机号还不足以登录。首次注册和之后的每次验证码登录，都需要验证本次有效验证码；本设备已有有效登录会话时，可以直接恢复学习页面。

## 2. 登录页面怎么呈现

保留现有桌面和手机 UI、固定配色以及学生独立入口。短信功能正式接通后，推荐默认展示“验证码登录”，旁边保留“密码登录”。目前短信尚未接通，本地仍保持密码登录可用。

验证码登录展示手机号、验证码输入框、“获取验证码”按钮和“登录 / 注册”主按钮。表单明确告知：“未注册的手机号验证后将自动注册。”用户协议、隐私政策放在对应操作附近，并按实际产品规则记录用户同意情况。这个说明用于解释操作后果，不恢复此前删除的本地存储小字。

保留现有顶部“注册”入口时，让它复用同一套验证码流程，避免维护两套注册机制。既有的注册成功 UI 可用于新用户首次验证成功后的欢迎步骤，主按钮改为“开始学习”；不用要求刚验证成功的用户再登录一次。

密码登录展示手机号、密码、“记住密码”、登录按钮和“忘记密码？”入口。学员端不出现管理员入口、管理员角色名称或管理功能。

## 3. 验证码登录完整流程

1. 输入手机号。中国大陆 11 位手机号在程序内部统一转换为 `+86` 国际格式，同一手机号不能因写法不同产生两个账号。
2. 点击“获取验证码”。先验证号码格式和发送频率，再请求发送短信。只有发送请求被服务接受后，才显示“验证码已发送”并开始重发倒计时；这不代表运营商已经送达。
3. 输入 6 位验证码并点击“登录 / 注册”。由 Supabase 验证号码与验证码是否匹配、是否过期、是否已使用。
4. 验证成功后，取得真实登录会话并检查对应学员资料。已有学员复用原账号的唯一 ID；新学员完成资料初始化。未成功验证的记录不计为已激活学员。
5. 新学员看到欢迎界面，昵称可以稍后补充；已有学员直接进入学习页面。账号姓名、手机号、学习记录都读取该账号对应的数据。

建议初始参数：重发间隔 60 秒，验证码有效期 5 分钟；具体值以最终服务端配置为准，界面文案保持一致。验证码成功使用后失效，更换手机号需重新申请。对同一手机号、来源 IP、全站发送量设置服务端限制，并接入人机验证和短信费用上限。页面倒计时只负责展示，限流需要在服务端生效。

## 4. 未设置密码的账号怎么处理

验证码注册成功时，不生成默认密码，不把验证码当作密码，也不要求用户立即补设密码。

用户可以始终通过验证码登录。在“我的 → 账号与安全”中展示“登录密码：未设置”和“设置密码”按钮。设置时确认当前登录身份，必要时重新验证短信，再输入新密码与确认密码；当前产品规则为最低 6 位，并让前端规则与 Supabase 配置一致。

设置成功后提示：“密码设置成功，下次可使用手机号和密码登录。”原来的验证码登录仍然可用，账号 ID、会员信息、收藏和学习记录都不变。

“忘记密码？”进入验证码验证流程，验证通过后重设该账号密码。流程只恢复已有账号，应关闭这个入口的自动注册，避免把找回密码误做成新注册。验证码登录入口则允许自动注册。

## 5. 状态和提示文案

| 实际情况 | 建议行为和文案 |
| --- | --- |
| 新手机号验证码验证成功 | 自动完成首次注册：“注册成功，欢迎加入 Eastudy。” |
| 已注册学员验证码验证成功 | 登录原账号，进入学习页面 |
| 本人身份已验证，并确认没有设置密码 | “该账号尚未设置密码，请使用验证码登录。”；提供“设置密码”入口 |
| 密码认证失败，接口未说明具体原因 | “账号或密码错误”；同时提供“验证码登录”入口 |
| 通过可信流程确认账号尚未注册 | “该账号还没有注册，请先注册。” |
| 手机号格式错误 | “请输入正确的手机号。” |
| 验证码不匹配或已失效 | “验证码错误或已失效，请重新获取。” |
| 验证码请求过于频繁 | “操作过于频繁，请稍后再试。” |
| 短信发送失败 | “验证码发送失败，请稍后重试。” |
| 网络失败或读取账号资料失败 | “登录服务暂时异常，请稍后重试。”；不得自动新建第二个账号 |
| 身份存在但不允许进入学员端 | 不进入学习页，也不向用户展示管理端信息；给出通用登录失败提示，后台记录实际原因 |

Supabase 官方说明，密码认证返回的错误可能不区分“账号不存在”“密码错误”和“账号不支持该登录方式”。因此，不能仅凭一次密码登录失败，就准确提示未注册或未设置密码。推荐在未验证身份时统一提示认证失败，用户切换验证码并验证成功后，再展示准确的密码设置状态。不增加公开查询任意手机号是否注册、是否设置密码的接口。

本次按要求替换的旧文案位于“认证之后检查学员资料/角色”的分支。这个分支也可能因为资料缺失或角色不匹配触发。文字替换已经完成，但它本身没有新增准确判断“未注册”的功能；正式实施本方案时，应拆开这些情况，避免让已有账号重复注册。

## 6. 数据库怎么组织

Supabase Auth 管理身份、密码哈希、验证码验证和登录会话。应用数据库管理学员资料、课程和学习数据。不要把明文密码或短信验证码写进学员资料表、统计表或日志。

| 数据 | 建议保存位置与规则 |
| --- | --- |
| 登录手机号、密码凭据、认证身份 | Supabase Auth 管理；密码由 Auth 校验 |
| 学员昵称、头像、资料 | `public.profiles`，ID 关联 Auth 用户的唯一 ID |
| 学员角色 | 服务端初始化为 `student`；学员不可自行修改角色 |
| 学员激活状态、实际短信验证时间 | 服务端控制的状态或字段；只有验证成功才激活 |
| 是否设置密码 | 服务端根据真实认证状态维护，只允许本人读取；不是密码校验依据 |
| 学习记录、词汇、收藏 | 使用该用户唯一 ID 关联；通过 RLS 限制本人访问 |
| 新增学员、活跃人数 | 统计已激活学员，排除只申请验证码但未完成验证的记录 |

Supabase 可能在申请验证码阶段就产生待验证的 Auth 记录，现有新用户触发器也可能同时创建 `profiles`。所以“表里已有一行”不等于“已经注册验证成功”。实施时要检查现有触发器，以验证完成作为学员激活、首次注册统计和学习访问的依据。

同一个手机号在重发、重复提交、切换登录方式时应始终对应同一用户 ID。资料初始化需要可重复执行，已有资料只能补缺失字段，不覆盖原昵称、角色、会员状态和学习数据。

管理员继续走独立管理端登录与权限校验。验证码流程不得把原管理账号降级成学生，也不得借此产生管理员权限。浏览器会话键分离之外，数据库权限也必须校验身份。

## 7. 记住登录怎么工作

继续保留密码模式下的单个“记住密码”选项，其实际功能是保存本设备登录会话，不保存输入的密码。验证码模式可把同一个选项显示为“保持登录”，不增加第二个“记住账号”选项。

勾选后，同一设备、同一浏览器、同一网站地址下，有效会话可以在下次访问时恢复；身份资料检查成功后直接进入学习页面。登录过期、主动退出、清除浏览器数据或换设备后，需要重新验证。

取消勾选后使用标签页会话存储。浏览器的“恢复上次会话”功能有时会恢复标签页数据，因此不能承诺所有浏览器一关闭就强制退出；共享设备应主动点击“退出登录”。

会话记录不会由网站主动同步到另一台设备。但正常登录、验证和续期仍需要与 Supabase 通信；本机存储不代表登录过程中没有网络请求。后续切换正式域名时，由于网站地址改变，学员需要重新登录一次。

## 8. 当前项目实施状态

1. 接通短信发送：在 Supabase 配置短信供应商，或者通过 Send SMS Hook 接入适合目标手机号地区的服务。服务器保存服务商密钥，前端只用公开配置。
2. 已接通验证码客户端流程：普通登录使用 `signInWithOtp` 并允许新用户创建；通过 `verifyOtp` 的 `sms` 类型验证成功后，复用现有学员资料读取和学习页跳转逻辑。找回密码入口禁止自动注册。
3. 已调整现有界面：启用验证码获取按钮、60 秒倒计时、提交状态和错误提示；复用桌面和手机注册成功 UI。短信模式不会执行密码长度校验。
4. 已补齐账号与安全：提供设置密码、重设密码；通过认证后的 `updateUser` 更新密码。新密码最低 6 位。
5. 已提供数据库迁移：新增密码状态、本人标记密码 RPC，并保护角色和密码状态不被浏览器直接篡改。迁移仍需在 Supabase 执行。
6. 处理历史账号：原 MVP 允许未验证手机号直接密码注册；历史的自动确认标记不等于实际短信验证。切换时需检查现有号码与资料归属，保留原用户 ID；涉及原有学习数据或会员权益的归属争议不能只凭当前手机号直接合并。需要实际短信验证时，配置也应切换到相应验证模式。
7. 本地完成真实短信联调和回归，交付可检查版本，再按已经约定的本地检查流程安排上线。

## 9. 免费版和短信费用

Supabase 免费版可以作为该 MVP 的认证与数据库基础；官方 Auth Hooks 页面目前也将 Send SMS 列为 Free、Pro 可用。数据库、调用和函数执行仍受所选套餐配额约束。

短信服务需要单独配置，并按供应商实际价格付费。面向中国大陆手机号，需要核实供应商支持范围、短信签名与模板要求、申请主体资质以及实际到达率，不能只根据“API 请求成功”判断用户能收到短信。

现阶段不预算真实短信费用时，可以继续使用已经接通的手机号＋密码方案；验证码分支仅用于隔离的测试环境验收，不作为已向用户开放的能力。本次文案修改未开启或计费发送短信，也未改变 Supabase 线上配置。

## 10. 实施后的验收场景

- 新手机号：只申请短信不进入学习页、不计入已激活学员；验证成功后只产生一个有效学员账号。
- 已有手机号：验证码登录与密码登录读取同一份昵称、会员状态和学习数据。
- 未设置密码：验证码登录正常；密码方式不允许进入；设置密码后两种方式均可登录。
- 无效验证码、重复使用、过期验证码、发送过频、断网、资料请求失败：提示正确，不误判为新用户，不泄露验证码。
- 刷新、重发、重复点击和多标签页：不重复建档、不重复计入注册统计。
- 管理账号访问学生入口：不进入学生学习页，不改变账号角色，不显示管理内容。
- 勾选与取消记忆、主动退出、令牌过期、换设备：行为符合本方案；手机和桌面均能输入、切换及提交。

## 11. 官方调研来源

1. [Supabase：Phone sign-in](https://supabase.com/docs/guides/auth/phone-login)——验证码无密码登录、短信供应商、验证码发送与验证。
2. [Supabase：signInWithOtp](https://supabase.com/docs/reference/javascript/auth-signinwithotp)——短信登录请求接口。
3. [Firebase：Authenticate with Firebase with a Phone Number Using JavaScript](https://firebase.google.cn/docs/auth/web/phone-auth?hl=en)——短信验证后登录、首次登录建立账号、人机验证。此次访问使用其官方中国镜像。
4. [Supabase：signInWithPassword](https://supabase.com/docs/reference/javascript/auth-signinwithpassword)——密码登录错误不一定区分账号不存在与密码错误。
5. [Supabase：updateUser](https://supabase.com/docs/reference/javascript/auth-updateuser)——登录后的密码更新能力。
6. [Supabase：Auth Hooks](https://supabase.com/docs/guides/auth/auth-hooks) 与 [Send SMS Hook](https://supabase.com/docs/guides/auth/auth-hooks/send-sms-hook)——免费版可用范围与接入自有短信供应商。
7. [Supabase：Rate limits](https://supabase.com/docs/guides/auth/rate-limits) 与 [Password security](https://supabase.com/docs/guides/auth/password-security)——服务端频率限制与密码策略。

## 12. 本次已完成的本地修改

- `assets/js/app.js`：接通验证码发送、验证、自动注册、找回密码和会话恢复，并将指定提示改为“该账号还没有注册，请先注册。”
- `shared/supabase-client.js`：增加 `signInWithOtp`、`verifyOtp`、学员资料初始化和登录后设置密码。
- `index.html` 与 `assets/css/app.css`：完成桌面和手机验证码表单、倒计时提示及账户内设置密码 UI。
- `supabase/migrations/20260907_otp_and_password_status.sql`：新增密码状态与字段保护；尚未在远端执行。
- 已通过 JavaScript 语法、认证契约、整包审计和 Google Chrome 桌面/手机界面检查；没有发送真实短信。
- 修改前的两个文件备份位于：`E:\英语网页制作\本地修改备份\login-copy-20260907-200637`。这是本次修改前的本地副本，不会丢失此前未提交的修改。
- 验证码程序和密码设置已在本地完成；短信供应商、远端数据库迁移和线上部署尚未执行。
