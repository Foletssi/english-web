# Eastudy Beta6.21.0 本地迭代记录

## Beta6.21.1 登录兼容修复

- 线上数据库的新学员角色实际为 `learner`，旧前端只接受 `student`，因此 Supabase 已认证成功后仍会被学生端拒绝。
- 学生端现同时接受 `learner` 与历史 `student`，但继续严格拒绝 `admin`，学生端与管理端隔离不变。
- 新建资料统一写入数据库当前使用的 `learner` 角色，学习进度、收藏和生词同步也兼容两种学员角色。

## Beta6.21.2 登录响应优化

- Supabase 密码验证和角色检查通过后立即进入学生首页，学习记录在后台继续同步，避免网络较慢时看起来像“点击登录没有反应”。
- 登录状态恢复同样先显示页面，再水合当前账号数据；数据库状态标记 RPC 改为非阻塞调用。

## 本轮完成

- 首页增加“今日学习 / 继续上次学习”优先区，新账号从 0 开始，历史账号读取本人真实进度。
- 首页视频、合集、最新内容、学习天数、学习时长、连续天数和收藏数量改为数据驱动。
- 学习缓存按 Supabase 用户 ID 隔离；进度、收藏句、生词与复习状态接入现有 Supabase 写入接口。
- 播放页拆分“字幕显示”和“练习方式”，增加倍速选择、句后暂停、单句循环锁定与手动滚动后的“回到当前句”。
- 视频无字幕时停止复用其他视频字幕，并禁用依赖字幕的练习入口。
- 挖空支持多词短语并忽略大小写、首尾空格等无意义差异。
- 跟读改为真实浏览器录音、回听和重新录制，不再显示固定模拟评分；录音不上传。
- 视频结束增加学习结果、收藏复习、返回首页和继续合集入口。
- 首页轮播圆点增加可访问名称和选中状态。

## Image 2 设计依据

本轮字幕高亮和会员账户视觉继续采用项目已有的 Image 2 设计参考，并把参考图归档在 `design/image2-references/`。新的首页 Hero 提示词已经按“英国乡村清晨 Vlog、人物居右、左侧留文案空间、无文字/Logo/水印”完成，但当前配置的 Image 2 API 通道没有可用模型，未把失败结果写入项目，也没有用其他模型冒充。

## 数据库说明

`user_progress`、`saved_sentences`、`user_vocabulary` 和 `study_events` 已接入。`daily_learning_stats` 的现有列结构尚未写入仓库，因此本轮学习分钟继续使用当前用户专属的本机缓存；在确认线上表字段前不猜列名，避免破坏已有数据。

## 本地验收

双击 `START_ZOSPEAK.bat`，或访问：

`http://127.0.0.1:8080/?local=beta6.21.0`

本版本只更新本地代码，没有执行 GitHub、Cloudflare Pages 或 Supabase 线上部署。
