# ZoSpeak Composite V1 Beta5

- 首页主题、语言、提醒与账户控制区移到右侧信息栏顶部。
- 中英切换更新为横向“中文 / EN”文本控件。
- 连续学习与本周分钟可进入“我的学习”查看时长数据。
- 热门标签会切换主页内容分类并定位到分类区域。
- 当前阅读词使用冷灰蓝扁平标记；标点保持在背景块外。

## Beta5.1

- 右侧顶部操作区收纳为 282px 右栏内的紧凑工具条，顶部与搜索框对齐，不再覆盖主视频区。

## Beta5.2

- 逐词定位框改为伪元素静态背景，不再改变文字的字重、字号、字距、行高或布局。
- 浅色：`#EAF0F6` / `#D5DEE8` / `#0F172A`；深色：`rgba(148,163,184,.18)` / `.24` / `#F8FAFC`。
- 普通阅读文字在浅色为 `#1F2937`，深色为 `#E5E7EB`。

## Beta5.5

- 强制刷新本地静态资源版本，避免浏览器继续使用旧标签/轮播脚本。
- 控制端发布闭环全部中文化。
- AI 设置新增“测试连接通路”，支持超时、状态码和 API Key 提示。

## Beta5.6

- 全局账号菜单补齐 VIP 标识、CDK 入口和到期时间。
- 标签使用严格分类结果，不再用其他分类内容覆盖点击结果；轮播脚本通过资源版本强制刷新。
- 控制端发布闭环文字全部替换为中文，并加入 API 测试连接通路按钮。

## Beta6.1

- 按 Image 2 视觉参考生成动态 VIP 组件：ZoSpeak Pro、到期时间、剩余天数与管理会员。
- VIP 卡接入全局账号菜单与首页账号菜单，CDK 更新后同步刷新。

## Video UI Iteration

- 以 Image 2 作为逐词阅读基准重排视频页桌面/移动布局。
- 浅色字幕正文提升为深石板色，非当前句不再褪色到不可读。
- 移动端保留词汇模式、字幕模式和所有播放器控制。

## Beta6.2 · Mobile video-first flow

- 移动端视频调整为首屏第一内容，随后显示“学习 / 字幕”横向切换与当前句卡片，符合单手观看习惯。
- 当前句、时间轴、播放控制与句子解析按视频 → 阅读 → 练习顺序排列；字幕页仍可独立滚动浏览。
- 修正主题按钮运行时 SVG 显示，浅色/深色均保持清晰焦点框与原重点词颜色。
- 逐词标记收紧右侧边界，标点不再被背景覆盖。

## Beta6.3 · Reference-aligned mobile hierarchy

- 根据用户提供的竖屏视频参考，将播放器提升到移动端首屏第一内容；学习/字幕切换紧跟播放器下方。
- 当前句卡片、时间轴、模式与解析区按观看路径依次展开，避免播放器被卡片挤到下方。
- 为底部五项导航预留滚动安全区，长字幕与解析内容可完整阅读。

## Beta6.4 · Category fallback continuity

- 热门标签在当前发布数据缺少完全匹配分类时，使用语义相关的已发布内容（如“旅行”关联海外表达），避免回退到无关的首屏视频。

## Beta 6.5 — 视频学习细节与个性化阅读
- 词汇模式接入真实字幕流：重点句聚焦、上下文保留、点击单词打开释义。
- 描边颜色加入可编辑色板与颜色选择器，且保留 CET/IELTS/短语原文字义颜色。
- 逐词标记使用可调描边变量，标点维持正文样式，不再覆盖语义色。
- 移动端字幕模式保留可拖拽视频进度条，视频→标签→进度→字幕顺序更符合观看习惯。
- 英文逐句字幕与当前句字号统一绑定阅读设置，桌面/移动端同步响应。
- 热门标签改为一体化 #标签按钮，点击后刷新分类视频与合集内容。
- VIP 徽章与账号菜单组件采用扁平金色标识，展示 CDK 对应到期时间。

## Beta 6.6 — 字幕歌词式滚动与可读性增强
- 当前句解析卡片字体改为跟随阅读字号令牌，默认提高标题与说明文字可读性。
- 移动端字幕面板改为独立滚动容器，固定在视频下方，支持触控滑动与细滚动条。
- 视频进度条继续只控制视频时间；拖动或播放时字幕自动滚动到对应句子并保持可见。
- 替换页面级 scrollIntoView，避免进度拖动造成整页跳动。
- 字号设置同步作用于桌面/移动逐句字幕、当前句和解析说明。

## Beta 6.7 — 双语当前句与精准字幕定位
- 修复移动端双语模式当前句只显示英文的问题，当前句卡片现在同步显示中文译文。
- 双语卡片高度随内容自然增长，长句换行完整，不缩放、不裁切。
- 字幕自动定位改用滚动容器相对坐标，当前句整行保持在可见区域内。
- 视频播放与拖动进度时采用即时定位，避免平滑动画滞后造成当前句暂时离开视口。

## Beta 6.8 — 词卡定位与阅读字号
- 词卡改为移动端安全底部卡片，桌面端按视口边界自动避让，内容支持独立滚动。
- 移除重复的“词汇”模式入口，保留逐词点击查看词卡与生词本功能。
- 解析、设置说明与备注字号提高，并继续跟随阅读字号设置。
- 首页标题、卡片、统计和标签字体整体增强，保持桌面与移动端可读性。

## Beta 6.9 — Eastudy 账户、短语卡与逐句语法
- 账户身份栏预留完整宽度，VIP 徽章与用户名称保持同排显示，菜单继续显示 CDK 到期时间。
- 首页与控制端品牌更新为 Eastudy，统一扁平 E 标识；关注按钮统一为清晰的描边操作组件。
- 字幕重点短语支持整段追踪、整段词卡与完整短语时间范围，标点保持在背景框外。
- 深浅色逐词追踪始终保留词汇语义颜色，避免播放状态覆盖重点词颜色。
- 每条示例句补充对应语法提示，控制端字幕编辑器支持录入和保存语法说明。
