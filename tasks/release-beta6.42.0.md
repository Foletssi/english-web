# beta6.42.0 发布与审计记录

日期：2026-09-16。用户授权本地完成后发布生产；code-review 基线为 559f267（beta6.41.0）。

## 发布内容

- 第3套雾蓝双色主题，保留第4/5版播放器布局；全宽视频、独立字幕滚动、112px底栏及真实安全区，图标18px、播放圆钮32px、点击区域44px。
- 四种练习模式展开，盲听独立，字幕设置放入更多；保留重点词颜色、短语整体下划线及逐词时间轴、视频目录和上/下一条。
- 修复词卡原句播放使用错误起点字段（start → s），增加设备朗读启动、失败与取消反馈。
- Supabase/HLS 浏览器脚本改为同源固定版本；目录按版本减少重复传输，封面增加授权后的边缘缓存；会员检查始终优先于缓存。
- 云端偏好使用字段补丁和行锁合并；设置按账号持久化重试，手动标记已学独立保存且不增加观看时长。

## Standards

按照 code-review 的规范轴检查本次差异。当前 agents=0 约束下由主代理独立完成两轴审查。项目没有 issue-tracker 配置，使用已确认设计文档作为需求来源。

已核对 AGENTS.md 的部署顺序、临时文件排除、R2 禁删要求；新增函数限制 authenticated 调用并实时验证会员；用户补丁按 auth.uid() 定位；媒体缓存不得绕过授权。未发现尚未处理的阻断性规范问题。既有 app.js 单文件与紧凑格式维护成本较高，本轮仅在对应功能处改动，不进行无关重构。

## Spec

需求来源：docs/design/player-blue-spec-20260916/IMPLEMENTATION.md、SPEC.md 及本轮确认的速度、发音和账号同步要求。已核对模式入口、紧凑尺寸、颜色、横竖屏、词卡、更多、连播与上一条、学习标记的事件映射。审查中发现的原句起点字段错误已修正并补充交互回归。未发现本轮发布范围内尚未处理的阻断性需求问题。

旧视频的新教学候选已生成但尚未人工确认发布，不能把它们宣称为线上词表。既有发布内容仍可能显示旧重点词；本轮不绕过教学审核。设备 TTS 依赖浏览器/系统英语语音能力，并非新增在线云端 TTS。

## 验证

- `npm run test:all` 通过；最后修复原句起点后再次运行 `node audit/player_loop_ui_test.mjs` 通过。
- UI 回归覆盖10种视口、精确尺寸、安全区模拟、深浅主题、模式/字幕/词卡、学习标记、自动连播及上一条。
- 移动同步测试覆盖实时权限、版本缓存、换号、发音启动/取消/超时、失败重载恢复、请求中继续编辑。
- 媒体路由测试覆盖 HLS/封面缓存命中、私有响应、VIP过期时在缓存前拒绝、范围请求。
- 官方 CLI 已成功应用 `20260916100000_mobile_sync_v1.sql`；云端事务测试返回 `mobile_sync_transaction_passed_rolled_back`，确认实时权限、版本响应和字段合并，测试写入全部回滚。
- 排除原样保存的第三方 `assets/vendor/*` 后，暂存差异空白检查通过，最终 app.js 语法检查通过。上游 Supabase 文件第7行模板字符串包含尾随空白，保留原文件以避免改变语义；线上静态资源核对结果待部署后补记。

本地界面测试使用测试数据与媒体替身；不能证明真实 iOS/Android/微信解码、实际发音、所有网络线路或所有设备无缺陷。

## 发布顺序与恢复

按项目授权降级通道执行官方 Supabase CLI 迁移及事务验证，再提交推送 main，等待 Cloudflare 连接 Git 自动部署，最后 HTTPS 验证。Chrome 连接器曾返回 `unsupported Codex auth method: apikey`，不使用桌面/CDP替代部署。

发布前不删除媒体或教学数据。回退前端可重新部署 559f267；新增 RPC 与旧前端兼容，可保留。事务测试最后 ROLLBACK，不保留测试账号偏好修改。临时日志、缓存及设计原型不进入部署提交。

当前状态：已提交并推送 main，发布提交为 `faf9ef3`；Cloudflare 连接 Git 部署已通过线上 HTTPS 验证。

## 线上验收结果

- 学生端 `/`、管理端 `/admin/` 均返回 HTTP 200，HTML 包含 `beta6.42.0`。
- `node tmp/verify-release-6400.mjs`（期望版本已设为 beta6.42.0）最终退出码为0：32/32 页面及引用的 JS/CSS 与本地发布内容一致，比较时仅统一换行符，不生成摘要。首次检查发生在部署切换前，后续完整检查全部通过。
- HTML 响应为 `no-cache, no-store, must-revalidate`；固定版本 Supabase 脚本为 `public, max-age=31536000, immutable`，类型为 `application/javascript`。
- 未携带播放会话访问540P播放清单，返回 HTTP 401 与 `PLAYBACK_SESSION_REQUIRED`，响应 `Cache-Control: no-store`。
- 本次没有完成真实手机、真实会员登录后的视频解码及系统语音实测；32项资源一致不等同于所有设备端到端功能保证。两个视频的新教学候选仍待人工审核发布。

本段为部署后本地补记，未再次提交触发无必要的生产构建；线上发布代码对应 `faf9ef3`。
