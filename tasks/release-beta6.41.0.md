# beta6.41.0 教学与手机播放器

基线：fd7c075。对应 docs/M08_DEEPSEEK_TEACHING_PROMPTS_20260915.md、docs/M04_M08_MOBILE_TEACHING_ITERATION_20260915.md。

- [x] 新上传：原始词片段、语义分句、覆盖校验、接缝复核、阶段断点
- [x] 成人教学提示词、上下文、人工锁定校验、版本和重选差异
- [x] 学生端统一已审核表达、空集合、词头去重、短语下划线与逐词跟随
- [x] 手机底栏、视频目录、视频切换、独立更多面板、计数、中文模式
- [x] 本地回归及 Standards / Spec 审计
- [x] 生产前向迁移 `20260915210000_teaching_review_v7.sql` 已执行并验证
- [x] 经用户明确授权，隐藏重启 Eastudy Cloud Worker，确认新 Python 进程及 540P/Whisper/DeepSeek 启动自检
- [x] Git 关联的 Cloudflare Pages 发布与线上资源逐文件验证
- [x] 两个旧视频的教学重选候选生成与不变量验证（不绕过人工审核）

保留540P媒体与旧字幕ID，不删除用户学习记录或R2对象。

## 实现与验证

新增 `shared/mobile-player.js` 复用原播放、队列、收藏处理函数；新增 `admin/assets/teaching-editor.js` 提供可编辑词卡、增删、差异与待复核确认。AI 指令集中在 `teaching_prompts.py`，版本为 `adult-vlog-v7-20260915`；`segmentation.py` 负责词范围、时间和分批接缝校验，不让模型编写时间。旧视频只重选教学内容，不重新切分或替换媒体。

`npm run test:all` 已退出 0：静态检查 71/71、鉴权/VIP/邀请码/媒体会话、学习队列与离线同步、播放器与管理端 UI、删除流程、78 项云端契约与 23 项 worker Python 测试、47 项 local-studio Python 测试、模块边界、内容检查、合集映射与16项学生端映射场景。新增结构化教学编辑器浏览器测试已单独通过，并接入 `learner-admin-test`。播放器回归覆盖8种视口，包含320px窄屏、390px竖屏及844px横屏的面板几何、44px触摸区域和焦点返回。

## Standards

以 `fd7c075` 为固定基线，按 code-review 的 Standards 轴审查工作区最终差异。遵守模块职责，新增移动交互和教学编辑模块复用原业务处理函数；不改登录、会员、媒体规格和删除授权。用户已授权生产迁移、提交与部署。运行约束禁止子代理，因此两条审查轴由主代理分别执行。

未发现尚未处理的阻断性规范问题。`git diff --check` 通过；换行格式提示仅为仓库 CRLF 设置提示。未将临时目录、缓存或凭据纳入发布文件。

## Spec

两份方案的新增上传语义分句、成人表达筛选、有效空集合、人工锁定、版本防串写、审核差异、短语整体下划线、逐词跟随、移除三点入口、固定底栏、独立更多弹层、视频目录及前后视频切换均已有实现和相应回归。

未发现尚未处理的阻断性需求偏差。旧视频重选只进入草稿 REVIEW，线上已发布教学内容保留，须在控制端核对候选后发布。自动化 Chromium 视口测试不能代表所有真机；iOS、微信浏览器及实际英语教学质量仍需人工验收。无可信考试词表时不虚构“四级/雅思词表来源”。

## 部署、备份与回退

浏览器连接器不支持本会话的 API-key 登录，按仓库已授权替代通道使用官方 Supabase CLI、Git 与 HTTPS 验证。发布顺序：迁移验证 → main 提交推送 → Cloudflare 关联 Git 构建 → 学生端/控制端 HTML 与全部引用的本地 JS/CSS 逐文件正文比对。

处理前快照在本机 `tmp/teaching641-before.json`，revision 70；不得提交快照。回退应用时使用兼容当前教学数据的旧提交，保留前向迁移、用户新增学习记录与媒体对象。候选可参考处理前快照人工恢复，不执行破坏性数据库降级。

## 最终验收证据（2026-09-15）

- 应用提交 `091e493` 已推送 main，生产版本 beta6.41.0；学生端/控制端共29份HTML、JS、CSS通过HTTPS正文一致性检查，29/29。记录：本机 `tmp/production-641-verification.json`，UTC 13:53:19。
- 最终全量测试重新执行并退出0，日志：本机 `tmp/test641-final.log`。此前一次失败原因为本地8765测试服务器未启动；启动仅监听127.0.0.1的服务器后重新跑完全套，未修改代码规避测试。
- 重启后worker进程57072，启动时间北京时间21:44:24，MainWindowHandle=0；Whisper/CUDA、DeepSeek及balanced-540-v1自检正常，实际云端任务持续产生进度。计划任务配置Hidden、IgnoreNew、无执行时限、每分钟失败重试，运行状态Running。重复触发记录的LastTaskResult为0x800710E0，不能据此声称已实测重启电脑后的恢复；本次已验证的是隐藏启动及真实任务执行。
- 新教学内容只写入草稿，须在控制端“视频库 → 视频详情 → 编辑字幕”检查新增/移除/保留/待复核差异，必要时编辑后确认，再发布到学生端。自动生成不是人工教学质量验收；抽样仍存在值得人工判断的选词。

### 两个旧视频的最终结果

生产快照 revision 74 已读取并与处理前 revision 70 比较。已发布内容完全一致；两个视频的句子数量、ID、英文、起止时间、逐词时间、已有中文、人工锁定和媒体字段通过不变量校验。候选使用 `adult-vlog-v7-20260915`，全部进入 REVIEW；原选词保留于差异记录。校验脚本及快照仅存于本机 tmp，不提交生产仓库。

| 视频 ID | 句子数 | 原表达条目 | 新候选条目 | 无需重点词的句子 | 待复核表达 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1788926081632 | 211 | 519 | 209 | 52 | 5 |
| 1789024924932 | 262 | 483 | 149 | 133 | 1 |

以上为逐句表达条目数，不是词头去重数。允许没有合适重点词的句子输出空集合，不为凑数添加基础表达。473句全部带新提示词版本，数据库 `private.learning_sentence_issues_v5` 结构校验异常为0。结构通过不等于教学质量已获人工确认。

- 任务 `0ebcecd5-0a18-46a1-9a32-45e9138a1005`：REVIEW、100%、无错误。
- 任务 `4eeb1225-2882-43b9-ac6a-fea4b29d7785`：REVIEW、100%、无错误。
- 最终快照读取首次因CLI临时登录角色认证失败退出；重新通过官方CLI建立连接后成功，未修改数据库凭据或应用鉴权逻辑。

审计结论：Standards未发现未处理的阻断性问题；Spec未发现未处理的阻断性偏差。人工教学复核及iOS/微信真机验收仍是明确的验收边界，不承诺自动化测试可证明绝无缺陷。
