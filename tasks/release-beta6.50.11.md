# beta6.50.11 — AI 教学与上传链路性能迭代

## 范围

审计固定点：`94fa6c1`。本次只调整新任务的制作调度、AI 教学协议、成品上传和回执链路，不重跑或修改既有视频，不调用付费 AI 测试，不物理删除 R2 对象。

- AI 请求从整阶段独占改为全局 FIFO 的逐请求预算，默认 3、上限 4；保留生成后独立复核、否定、指代、多义词、IPA、翻译锁、源文疑点和跨句归属校验。
- 生产 Worker 启用 `detailReviewMode=delta` 与 `eligibilityContextMode=table`；缓存键隔离新旧协议，复核失败只重试复核单元。
- 网络请求从整阶段独占改为逐请求预算，默认 4、上限 8；重试退避在资源槽外执行，并记录排队、活动、请求、重试和字节指标。
- 语音上传每批最多 32 文件，同时保留单文件 1 MiB、批内 2 MiB、请求体 3 MiB 限制；Worker 全局最多 2 个语音批次，Pages 每批最多 2 个 R2 对象写入，总对象写入并发封顶 4。
- 新增批量授权、写入栅栏、写入确认和数据库回执 RPC。完整请求先验证再分配存储，部分失败只中止对应 multipart，成功项可恢复；删除确认仍受 durable fence 保护。
- 批量上传或批量回执接口不可用时，回退至已有逐文件协议。PUT 先 GET 对账，回执按相同 hash/size/etag 幂等重放；权限、租约和内容冲突不回退。

应用版本：`1.0.0-beta.6.50.11`。Worker 版本：`2.5.9`。

## 发布前验证

- `npm run test:all`：退出码 0；静态、契约、鉴权、学习、播放器、管理端、删除、云端处理、本地制作、模块边界和映射测试全部通过。
- Cloud Worker：42 项 Worker 单元测试及 11 项批量上传测试通过。
- 上传契约：32 文件限制、请求完整验证、当前 run 授权、丢响应对账、部分失败、删除栅栏、旧协议回退和全局 4 对象写入上限通过。
- AI 质量：delta/table 离线等价与边界样本通过；AI 用量账本、缓存隔离、取消和请求级 FIFO 调度通过。
- `git diff --check` 通过。

完整测试未发起真实模型请求，也未对生产视频建立任务。50% 总耗时和 10%–20% Token 降幅仍是后续同环境冷缓存验收目标；本次只确认实现、离线质量边界和故障恢复，不能把目标写成实测结果。

## Code review

### Standards

以项目 `AGENTS.md` 和通用代码异味基线审查。发现并修复两项发布阻断：批量协议缺少旧版回退；Worker 4 批次 × Pages 4 对象可能形成 16 路写入。另修复幂等重试在退避期间占用网络槽的问题。修复后未发现剩余阻断项。

### Spec

以本地性能方案和上传方案为规格源。批量 RPC、逐文件回执、删除栅栏、部分失败恢复、请求级公平调度、delta/table 质量门、缓存隔离和回退路径均已覆盖。真实冷缓存耗时、Token 与错误率的三轮对照需要新任务样本和付费模型授权，本次按约束未执行，也不宣称达标。

## 生产发布与回退

发布顺序：正式迁移 → RPC/权限验证 → `video-processing` Edge Function → GitHub `main` → Cloudflare Pages 精确 SHA → HTTPS 学生端、管理端和上传路由 → 空闲 Worker 更新为 2.5.9。

数据库迁移为前向兼容新增函数，不删除旧 RPC。应用回退可恢复上一提交并让 Worker 使用逐文件 v2 路径；数据库函数保留，避免回滚时破坏已完成回执。Worker 回退前先确认无活动租约。任何回退都不删除 R2 媒体对象。

## 生产发布记录（2026-09-21，北京时间）

- Supabase CLI 2.117.0 正式应用 `20260920160000` 与 `20260920161000`，远端迁移表与本地一致。迁移事务成功即包含四个函数的定义、撤销公开角色权限及 `service_role` 授权。
- `video-processing` Edge Function 从版本 8 升到 9，HTTPS 健康检查为 200、`ready=true`。使用 Worker 服务端身份调用 `worker-output-receipts-v3` 的空批次得到 `SUPABASE_400:OUTPUT_RECEIPTS_INVALID`，确认新 action、RPC 和服务端执行权限已贯通。
- 核心应用提交 `2829a8f88278e774ce77b51c0e98e7b9e1edb0f1` 已快进推送到 GitHub main，Cloudflare Pages 对该精确 SHA 显示成功。生产域名和该部署预览域名的学生端、管理端 HTML 均为 HTTP 200，并与本地发布文件逐字一致（只规范化 CRLF/LF）。
- 生产与预览的 `/api/processing/output` 均接受 5 个格式、大小和摘要正确的隔离假条目，随后在当前 run 授权阶段返回 `503 OUTPUT_STATUS_UNAVAILABLE`；这证明 32 文件批量路由已生效，且失败发生在分配 multipart/R2 写入之前。
- 同提交的独立 `Workers Builds: english-web` 检查失败；上一生产提交 `94fa6c1` 也有相同的 Workers 失败与 Pages 成功组合。正式发布通道是项目规定的 Cloudflare Pages，Pages 本轮成功，生产 HTTPS 与部署预览一致。
- 空闲 Worker 经 5 秒采样确认 CPU 和日志无变化后更新。旧 `2.5.7` 进程停止，新 `2.5.9` 进程启动；线上心跳确认 FFmpeg、Whisper、DeepSeek、`localInputV1` 就绪，计划任务运行中，启动错误日志 0 字节。
- 本轮没有创建或领取生产视频任务，没有重新处理旧视频，没有调用付费 AI，没有执行 R2 删除。
