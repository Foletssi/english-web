# beta6.43.0 合并修复与发布记录

日期：2026-09-16。前一网页版本：faf9ef3 / beta6.42.0。
审计基线：用户确认的559f267 / beta6.41.0，同时审查本轮未提交改动。
需求依据：docs/ITERATION_STATUS_AND_EXECUTION_20260916.md、docs/M02_M04_MOBILE_UI_IMAGE_REMEDIATION_20260916.md及用户追加的连续下划线、成人词汇筛选、旧视频更新和移除不完整发音要求。

## Standards

按code-review的代码规范轴审查。沿用本轮既定单代理审查限制，未启动子代理。检查模块职责、共用难度枚举、媒体鉴权、有限重试、账号切换竞争、迁移权限、快照并发控制、仅教学字段发布、私有数据输出边界及仓库发布顺序。

本轮修复后没有确认的阻断发布问题。数据库写入以revision和原始内容为前置条件；教学发布核对完整快照；图片恢复限制并发与重试，不绕过VIP权限。新增迁移为兼容性扩展，未物理删除R2媒体。没有为格式或命名偏好引入无关重构。

## Spec

按需求落实轴审查。手机双列小图、更多/偏好深浅主题、紧凑操作栏、连续短语下划线、逐词跟踪及固定词色已实现。图片响应式派生图、旧合集引用、独立会话续期和有限重试已实现。后台审核难度与学员筛选共用数据定义。新版DeepSeek指令已由处理服务加载；两个旧视频经独立审核后正式发布，去重重点词为59和40。合成发音及英美口音入口已移除，原句播放保留。

没有确认的遗漏项阻断本次发布。测试不证明每一个AI词汇选择均绝对正确；语义质量仍应持续抽查。实际手机的弱网与硬件解码未由本地模拟测试覆盖。

## 验证证据

- 完整npm run test:all通过，日志在本地tmp/test-all-643-final.log。
- 后续播放器UI回归通过，覆盖10种视口及深浅主题短语空格下划线像素验证。
- 处理服务单元测试23项通过，云端心跳记录adult-vlog-v9-20260916。
- 三项数据库迁移已应用；旧视频更新到revision84，发布前后完整快照核对通过。
- 两个视频的320px封面约5.0/7.3KiB、640px约10.7/16.1KiB、960px约16.5/24.4KiB。
- 发布流程：官方Supabase CLI验证后推送main，由连接Git的Cloudflare Pages构建，再通过HTTPS核对学员页、控制端及所引用资源。

## 上线结果

运行代码提交 `b88a181` 已推送main，Cloudflare连接Git部署完成。2026-09-16 12:04（北京时间）HTTPS验收：学员首页、管理页以及HTML引用的JS/CSS共33项，HTTP200且规范化换行后的全文与本地一致，两个入口均为beta6.43.0。私有验收结果：tmp/production-643-verification.json。

随后生产接口11项检查通过：两个视频各三档封面和实际540p/index.m3u8在未登录时返回401，session GET/POST未登录返回401，无效封面票据返回401；没有误报503。检查脚本最初请求不支持的master.m3u8返回400，已按实际540P播放清单路径更正测试，未为错误测试路径放宽服务端白名单。私有结果：tmp/production-643-api.json。

未使用真实用户密码或伪造用户身份，因此没有把匿名接口验收描述为已登录VIP端到端播放验收。真实iOS/Android弱网、微信浏览器和硬件解码仍需设备侧复核。

已检查明确暂存的39个发布文件、配置用途及私有产物边界；浏览器配置仅为公开publishableKey。密钥模式扫描的第三方SDK命中是其密钥类型检测代码，非实际私钥。tmp、supabase/.temp、缓存、工作器本地配置和无关设计产物未进入提交。旧数据备份路径及恢复规则见执行记录第六节。

两轴结论：Standards无已确认未修复阻断项；Spec无已确认未修复阻断项。真实设备及登录用户的生产端到端验证范围须单独说明。
