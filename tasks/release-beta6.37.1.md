# beta6.37.1 发布与审计记录

日期：2026-09-15。审计基线：24cd7c79e148a5121f77ce520a5fd941f8429508。

## 发布状态

- 已完成：全项目回归；Supabase `20260915110000_creator_catalog_lifecycle` 正式迁移。
- 待完成：main提交推送、Cloudflare Pages上线、HTTPS验收、空闲Worker隐藏重启及2.3.2心跳。
- 备用通道：Chrome连接器拒绝apikey；使用用户已授权的官方Supabase CLI、Git、Pages Git集成及HTTPS验证，不用桌面/CDP绕过。

## 用户可见修复

- 后台博主/合集保存失败不再假成功；删除有视频引用的博主必须明确选择接替者，云端原子更新公开目录和可恢复回收站，避免后台与学生端不一致。
- 收藏、关注、句子、生词、掌握、删除、复习在云成功后才更新本地；错误可重试，切换账号/页面时晚到结果不污染新页面。
- 词卡保存实际点击句的语境，不再误用当前播放句；收藏句等待视频metadata后正确跳转。
- 博主页视频/合集/简介标签能切换并支持键盘；失效博主/合集不残留旧内容；学习统计卡片跳转正确。
- 处理Worker遥测/上传/回执仅对瞬态错误有限重试，业务拒绝不吞掉、不盲目重试，上传进度节流上报。
- 已上线的540P及手机字幕跟随保留：两条媒体共199.364MB，相比251.211MB减少20.6%；不改字幕时间轴、不重复AI分析。

## Standards

双轴审计发现1项P2，已修复：草稿与公开版创作者引用不一致时，删除弹窗可能隐藏接替者选项并自动选人。现在始终显示选项、默认留空，后端要求接替者时保留弹窗，必须由管理员明确选择。定向失败复现后回归通过，模块边界检查通过。

## Spec

双轴审计发现1项P2，已修复：外层HTTP500包装的`SUPABASE_400:VIDEO_IN_TRASH`可能被当作网络瞬态错误重试或吞掉。现在解包原始状态/错误码，业务拒绝立即透传，只有已知瞬态错误有限重试；4项新增测试含16个子场景通过。

两轴结果：Standards 1项已修复，Spec 1项已修复；不合并两个轴的结论。扩展映射检查发现的个人收藏云写入及博主页标签问题也已修复。

## 验证证据

- `npm run test:all`：退出码0；静态审计71/71，Worker 23项、local-studio 39项测试通过。
- 学生DOM交互13项全部通过；管理端目录编辑/失败重试/显式接替/恢复/草稿隔离/手机宽度通过。
- 播放UI覆盖320×640、360×800、390×844、430×932、768×1024、1024×768、1280×800、844×390八个视口。
- 目录生命周期事务断言已在BEGIN/ROLLBACK中通过，未遗留fixture；包含权限拒绝、revision冲突、公开草稿隔离、回收站恢复及媒体引用保持。
- 全回归日志：`tmp/balanced-540/release-6371-regression.log`（本地证据，不提交临时目录）。
- 本地DOM测试拦截远程API，不向生产写入测试收藏/目录；媒体HTTPS验收仅使用临时登录会话并退出。

## 回滚与边界

- 上一生产网页为`a12886d` / beta6.37.0，Pages ID `65407113-f29a-4d24-83dd-48d012175926`。必要时回滚此次修复提交后推送，或恢复该已验证Pages部署；不reset用户工作区。
- 新RPC为增量兼容迁移，网页回退可保留该函数；不直接恢复整份旧快照以免覆盖管理员的新编辑。
- Worker可恢复前一提交的worker.py并在无活动任务时隐藏重启。生产媒体不回删、不重转码。
- 所有旧R2对象保留回滚，因此199.364MB是当前播放产物大小，不等于整个存储桶占用。
- 未实测真机iOS、微信内置浏览器及每个地区移动网络；不能保证所有网络绝不卡顿或绝对零缺陷。
- 既有Node模块类型提示、CRLF提示和Python HTTPError ResourceWarning未作为本轮无关重构扩大修改；本轮未新增依赖。
- 未运行固定8080的独立`deletion-browser-test`，不计入已测范围。
