# beta6.38.0 发布与审计记录

日期：2026-09-15。审查基线：2a46e95（上一轮生产后的记录提交）。规格：mobile-learning-iteration-20260915/plan.md。

## 发布状态

- 本地实现和最终全量回归已完成；正在提交并通过 Cloudflare Pages 的 Git 集成发布，生产证明待补充。
- 本轮仅前端和测试/规格变更，不需要 Supabase 迁移；不改 VIP、邀请码、处理 Worker、540P 配置或任何 R2 媒体对象。
- Chrome 连接器此前拒绝 Codex apikey 认证；按仓库授权备用通道使用 Git → Pages Git 集成 → HTTPS 校验，不使用桌面、CDP 或内置浏览器绕过。
- Git 命令单独指定已有代理 127.0.0.1:18809，不修改全局配置、不强推。部署前 fetch 确认 main 与 origin/main 一致。

## 用户可见变化

- 手机首页改为搜索、单张继续学习、分类与一个视频目录；ID 去重，每页12条，搜索/排序/队列使用完整已发布目录，返回保留筛选和位置。
- 底部视频／发现／生词／我的四入口；合集和博主集中到发现，原学习数据、收藏、历史及旧路由仍可访问。
- 手机播放页视频、字幕工具和句子/视频控制分区；字幕独立滚动，操作栏保持可见；横屏并排，词卡可独立滚动。
- 修正挖空占位、长词换行和字幕渲染；重点色及下划线保留，点词不触发句子跳转，手动浏览可返回当前句。
- 连播队列顺序稳定，提供上一条／目录／下一条及连播开关。首条不可向前，末条停止；A→B→A从0秒播放，历史学习记录保留。
- 跟读/停顿/填空优先于连播；未提交填空时手动切片先提示，取消保留输入；合集返回原合集，首页返回原筛选。

## Standards

按 code-review 双轴方法审查 2a46e95 至本轮工作区差异；遵守既有 agents=0/0 限制，由主代理分别审查两个轴。

未发现本轮尚未解决的阻断级仓库规范问题。实现复用既有播放器、目录、授权和学习 outbox；新增队列逻辑仍在共享模块，手机 CSS 限定作用域；外部文本保留转义，不新增依赖或第二套云端数据。原生 app.js/CSS 仍有历史集中与覆盖较多的维护负担，本轮未借 UI 修复重写全站。

## Spec

已修正本轮审查中发现的合集返回来源被通用点击绑定覆盖、同句 seek 清空手机填空、未提交练习直接切片、横屏视频导航触控过小等问题，加入对应 DOM 验证。需求中的真机、生产媒体网络耗时与真实跨设备检查仍未完成，不能用本地模拟测试宣称全部环境验收。

两轴结论：Standards 无已知未解决阻断项；Spec 本地功能验证通过，环境验证限制如下。

## 验证证据

- 最终 npm run test:all：退出码0。静态71/71，云Worker 23项、local-studio 39项；权限、票据、会话竞态、删除、处理、学习、模块边界和目录映射均通过。
- student_mapping_ui_test：14项全部通过，含收藏云失败重试、词卡上下文、收藏句 seek、合集队列和返回来源。
- player_loop_ui_test：320×640、360×800、390×844、430×932、768×1024、1024×768、1280×800、844×390；含15条不同视频与重复项、分页筛选、字幕跟随、固定控制、目录键盘、A→B→A、连播关闭及练习切换取消。
- 定向 student_player_interaction_test：9/9；npm run test:m04 通过；node --check assets/js/app.js 与 git diff --check 通过。
- 本地浏览器测试使用隔离的模拟API和媒体事件，不向生产写入测试记录。日志 tmp/release-6380-regression.log；截图 tmp/local-player-loop-mobile.png，不提交临时目录。
- audit/AUDIT_REPORT 为现有静态审计生成器输出，其旧版标题是历史固定标签；本轮版本和结果以本记录及 package.json 为准。

## 未实测与回滚

- 未实测 Android/iPhone/微信真机、系统软键盘、浏览器工具栏、安全区和放大字体；桌面视口模拟不代表真实手机全部通过。
- 本轮没有可用生产测试账号，未重新验证登录后两条540P媒体全链路及真实跨设备同步；上一版的媒体证明属于历史证据，不冒充本轮实测。
- 失败时回滚本轮应用提交至基线2a46e95对应产物，使用 git revert 生成反向提交并走同一发布流程；不 reset/强推，不覆盖学习数据，不删除媒体。
