# beta6.46.0 学习界面统一美化

基线：e473bc760ed01a8fab69d53d45e063996cf5bc2a（beta6.45.0）。方案：docs/design/study-surfaces-20260916.md。

## 完成内容

- 生词本：雾蓝深浅主题、紧凑统计条、搜索筛选、长词条释义及原句；保留列表/卡片切换、复习、掌握、删除及导出。
- 词卡：统一层次、标题渐变、原句区域和线性图标，保留真实发音、语境释义、复制、原句播放和收藏。头尾固定，中部滚动。
- 视频目录：真实封面、标题、难度、时长及当前项，沿用图片恢复、播放队列和前后视频逻辑。
- 重点词：直接呈现已有核心释义及原有重点色。盲听、听写模式使用同一字幕渲染器取色，避免依赖隐藏后的字幕节点；只看本视频筛选仅在生词本显示。
- gpt-image-2.5 生成四张深浅主题设计参考。上线使用响应式 HTML/CSS/SVG，不把大幅设计图作为网页素材下载。

## Standards

独立规范审查：0 项明确违规，0 项需报告的启发式问题。仅修改本次四个学习界面及相应验证。

## Spec

独立需求审查曾发现盲听/听写重点色丢失的 P2，已修复并复审关闭。普通、听写、盲听颜色一致性已有浏览器回归覆盖。

## 验证

- 静态审计 71/71，通过学习内容、队列、离线同步、账户隔离、词卡语境及发音故障恢复契约检查。
- 控制端和学生端目录映射契约通过。
- 播放器十种视口、深浅主题、连续下划线、固定底栏、词卡、循环与前后视频浏览器检查通过。
- 学生端映射 18 项通过。生词本 320/390/768/1024/1440px 深浅主题、列表/卡片操作边界均通过。
- 一次并行执行在生词本截图阶段超时；单独重跑全部通过，未将首次运行描述为全通过。
- 浏览器测试采用本地模拟内容和媒体事件，未替代实体手机网络、真实发音素材及会员登录端到端验收。

## 发布范围与恢复

本次无需数据库迁移和本地处理服务重启。不重新生成、改写或发布两个旧视频，不清理 R2。原有上传 AI 处理和故障继续入口保持。

沿用已授权的 Git main → Cloudflare connected-Git → HTTPS 核验通路；Chrome 连接器此前拒绝 API key 认证方式。发布后核对学生端、控制端及全部引用静态资源与本地一致。应用回退可恢复 e473bc7 对应版本，无本次数据库回退步骤。
