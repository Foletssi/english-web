# beta6.45.0 新上传教学与故障恢复

基线：cf58b70（beta6.44.0）。需求：new-upload-completion-20260916.md。

## 本次完成

- 词卡统一雾蓝深浅主题，固定标题及底部操作，中部可滚动；保留发音、收藏、复制和原句播放，缺失字段不占位。
- 难度全链路支持高考、专升本、四级、六级、专四、专八、雅思、托福；学生筛选展示实际发布视频具有的难度。
- 新上传沿用 540P、分句、口语化翻译、逐词语境义、DeepSeek 重点筛选、覆盖终检及发音生成。重点包含四级本身及以上的合格词语和俚语，不用简单连接词凑颜色。彩色重点和短语连续下划线保持。
- 任务提供“刷新状态”和“继续处理”，复用已完成检查点。上传失败保留清单，复用原片、记录和幂等键；正在提交时防重复操作。
- 已修正创建成功但网络丢失响应导致的版本冲突：读取最新版本，用同一幂等键恢复，不重复上传。
- 两个旧视频不重新生成、不改写、不发布，也不删除任何 R2 对象。

## Standards

code-review 独立规范审查：0 项明确违规，0 项需报告的启发式问题。规范轴发现的上传并发需求缺口已修正并复审关闭。

## Spec

code-review 独立需求审查：0 项未关闭问题。发现的创建响应丢失后无法恢复已修正，模拟服务器提交后丢失响应的回归通过。

## 验证及发布前置

- 静态审计 71 项；local-studio 85 项；cloud-worker 25 项；云端契约 78 项通过。
- 播放器多视口、深浅主题、词卡长内容、44px 操作区域、循环、前后视频及前后台映射通过；学生映射 17 项通过。
- 任务恢复、并发点击、网络失败、已接受请求但状态同步失败及上传响应丢失恢复均通过。
- 全套验证曾遇本地 8080 不可用及两处旧测试断言不符；切换 8766、等待弹层动画及更新真实例句断言后，受影响检查重跑通过。未将首次失败描述为一次全套通过。
- 官方 Supabase CLI 回滚验证成功，20260916220000 难度校验迁移已应用且远端迁移列表一致。迁移不写视频数据。
- 活动任务数为零后隐藏重启 Eastudy Cloud Worker；运行日志与远端心跳确认 adult-vlog-v10-20260916、balanced-540-v1、Kokoro 发音就绪。
- 发布采用已授权的官方 CLI → Git main → Cloudflare Git 部署 → HTTPS 核验；浏览器连接器此前因 API key 认证方式不可用。
- 本轮没有上传真实新视频或进行实体手机登录端到端测试；上述检查不替代这两项。新上传自动生成后仍进入原有发布审核。

## 恢复方式

应用代码通过恢复提交回到 cf58b70；数据库新增校验兼容原难度。处理服务按需隐藏重启。未改变旧视频数据，因此没有旧数据回滚步骤。

## 上线回执（本地验收记录）

2026-09-16 22:52（北京时间），生产提交 e473bc7 / beta6.45.0 已完成发布。首次检查处于部署切换期间（35/38），随后重新完整核验，学生端、控制端及引用静态资源 38/38 与本地一致；匿名及无效会话权限检查 11/11 通过。没有改写两条旧视频。此回执为部署完成后追加的本地验收记录。
